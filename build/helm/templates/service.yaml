apiVersion: v1
kind: Service
metadata:
  name: {{ include "das-boot.fullname" . }}
  labels:
    {{- include "das-boot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "das-boot.selectorLabels" . | nindent 4 }}
