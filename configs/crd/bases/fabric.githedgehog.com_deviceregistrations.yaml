---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.12.0
  name: deviceregistrations.fabric.githedgehog.com
spec:
  group: fabric.githedgehog.com
  names:
    kind: DeviceRegistration
    listKind: DeviceRegistrationList
    plural: deviceregistrations
    singular: deviceregistration
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DeviceRegistration is the Schema for the racks API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: DeviceSpec defines the properties of a rack which we are
              modelling
            properties:
              csr:
                type: string
              locationUUID:
                type: string
            type: object
          status:
            description: DeviceStatus defines the observed state of Device
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
